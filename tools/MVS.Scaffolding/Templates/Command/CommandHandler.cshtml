@inherits Microsoft.VisualStudio.Web.CodeGeneration.Templating.RazorTemplateBase
using MediatR;
using System.Threading;
using System.Threading.Tasks;

namespace @(Model.Namespace).Application.Command
{
    public class @(Model.CommandClassName)CommandHandler : IRequestHandler<@(Model.CommandClassName)Command, double>
    {
        public async Task<double> Handle(@(Model.CommandClassName)Command command, CancellationToken cancellationToken)
        {
            return await Task.FromResult(10.0);;
        }
    }
}