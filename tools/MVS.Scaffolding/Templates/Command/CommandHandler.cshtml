@inherits Microsoft.VisualStudio.Web.CodeGeneration.Templating.RazorTemplateBase
using MediatR;
using System.Threading;
using System.Threading.Tasks;

namespace @(Model.Namespace).Application.Command
{
    public class @(Model.ClassName)CommandHandler : IRequestHandler<@(Model.ClassName)Command, double>
    {
        public async Task<double> Handle(@(Model.ClassName)Command command, CancellationToken cancellationToken)
        {
            return await Task.FromResult(10.0);;
        }
    }
}